#include <stdio.h>
#include <stdlib.h>

typedef int *IntArr;
typedef struct {
    IntArr list;
    int length;
} Queue;

/**
 * @BhJaipal
 * TODO: Write functions to:
 * enque an element to Queue
 * depue an element from Queue
 * find an element in Queue, return -1 if not found 
 */
